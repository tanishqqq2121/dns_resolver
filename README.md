# dns_resolver
A custom, end-to-end recursive DNS resolver built from scratch in C++ using raw UDP sockets. Features low-level binary packet parsing, pointer compression handling, and a custom TTL cache.
